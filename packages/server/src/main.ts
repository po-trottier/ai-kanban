/**
 * @rivian-kanban/server — composition root: Fastify (REST + MCP + SSE + static SPA),
 * Slack Bolt (Socket Mode), croner jobs. See docs/architecture/overview.md.
 * Populated via TDD (tasks: REST API, MCP server, Slack adapter).
 */

export {}
